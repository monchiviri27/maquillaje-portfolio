---
import Layout from '../layouts/Layout.astro';
import Button from '../components/ui/Button.astro';

// 1. DATOS DE CONFIGURACIÓN (Solo debes modificar estos valores)
const bioData = {
    // 🖼️ RUTA CRÍTICA: Cambia esta ruta a tu foto de perfil
    image: '/assets/tres.jpg', 
    name: 'Andrea Bazurto',
    tagline: 'Maquilladora Profesional y Especialista en Belleza Nupcial',
    mission: 'Mi misión es crear looks que no solo embellecen, sino que empoderan. Cada pincelada busca resaltar la singularidad de mis clientas, garantizando un resultado que perdura y que se siente 100% auténtico.',
    experience: '8+ años de trayectoria en el sector.',
    focus: 'Maquillaje nupcial de larga duración, corrección de color avanzada y servicios a domicilio.',
};

// Función para asegurar la ruta de la imagen desde /public
const getImagePath = (path: string) => (path.startsWith('/') ? path : `/${path}`);
---

<Layout 
    title={`Sobre ${bioData.name} | Historia y Filosofía`}
    description={`Conoce la trayectoria y filosofía de ${bioData.name}, maquilladora profesional especializada en looks nupciales y sociales.`}
>
    <section class="pt-32 pb-16 px-6 bg-pink-50 text-center">
        <h1 class="text-6xl title-font font-extrabold text-gray-900 mb-4" transition:name="page-title">
            Detrás del Pincel
        </h1>
        <p class="text-xl font-medium text-pink-600 max-w-4xl mx-auto mb-8">
            {bioData.tagline}
        </p>
    </section>

    <section class="max-w-7xl mx-auto px-6 py-20">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-12 items-center">
            
            <div class="lg:col-span-2 order-2 lg:order-1 lg:sticky lg:top-24">
                <div class="aspect-[4/5] rounded-xl overflow-hidden shadow-2xl"> 
                    <img 
                        src={getImagePath(bioData.image)} 
                        alt={`Retrato profesional de ${bioData.name}`}
                        class="w-full h-full object-cover"
                        loading="eager"
                    />
                </div>
            </div>

            <div class="lg:col-span-1 order-1 lg:order-2 pt-6 lg:pt-0">
                
                <h2 class="text-4xl title-font font-bold text-gray-900 mb-6">
                    Mi Trayectoria
                </h2>
                
                <p class="text-lg text-gray-700 leading-relaxed mb-6">
                    Mi viaje en el mundo del maquillaje comenzó hace **{bioData.experience.split('+')[0].trim()} años** y se ha enfocado en la belleza que perdura y se siente auténtica. Me especializo en realzar la belleza natural, dominando técnicas de **{bioData.focus}**.
                </p>

                <p class="text-lg text-gray-700 leading-relaxed mb-8">
                    La belleza no es disfrazar, sino iluminar. Cada cita es una colaboración donde tu comodidad y tu visión son la máxima prioridad.
                </p>

                <h3 class="text-3xl title-font font-bold text-gray-900 mt-10 mb-4 border-b pb-3">
                    Mi Compromiso
                </h3>

                <ul class="space-y-4 text-lg text-gray-700">
                    <li class="flex items-center">
                        <span class="text-pink-600 mr-3">✓</span>
                        Uso exclusivo de productos de alta gama y larga duración.
                    </li>
                    <li class="flex items-center">
                        <span class="text-pink-600 mr-3">✓</span>
                        Puntualidad y atención personalizada el día de tu evento.
                    </li>
                    <li class="flex items-center">
                        <span class="text-pink-600 mr-3">✓</span>
                        Transparencia total en precios y servicios ofrecidos.
                    </li>
                </ul>

                <div class="mt-10">
                    <Button href="/contacto" variant="primary">
                        Agendar Consulta
                    </Button>
                </div>
            </div>
        </div>
    </section>

    <section class="bg-gray-800 text-white py-16 px-6">
        <div class="max-w-4xl mx-auto text-center">
            <h3 class="text-3xl title-font font-bold mb-4 text-pink-400">
                Nuestra Filosofía
            </h3>
            <blockquote class="italic text-2xl font-light leading-relaxed">
                "{bioData.mission}"
            </blockquote>
        </div>
    </section>

</Layout>